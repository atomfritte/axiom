name: axiom_tools
description: Validator, Layering-Check und Szenario-Runner fuer AXIOM.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  axiom_core:
    path: ../packages/axiom_core
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
